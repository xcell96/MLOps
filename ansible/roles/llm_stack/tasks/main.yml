- name: Create directory
  file:
    path: /opt/llm
    state: directory

- name: Copy compose file
  copy:
    src: "{{ playbook_dir }}/../compose/compose.yaml"
    dest: /opt/llm/compose.yaml

- name: Start stack
  command: podman-compose -f /opt/llm/compose.yaml up -d
